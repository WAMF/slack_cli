name: slackcli
description: A CLI for posting messages, replying to threads, and sending DMs in Slack.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.7.0
  cli_completion: ^0.5.1
  http: ^1.3.0
  mason_logger: ^0.3.3
  pub_updater: ^0.5.0

dev_dependencies:
  build_runner: ^2.10.5
  build_verify: ^3.1.1
  build_version: ^2.1.3
  mocktail: ^1.0.4
  test: ^1.29.0
  very_good_analysis: ^10.1.0

executables:
  slackcli:
